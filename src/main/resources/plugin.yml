name: ${name}
description: ${description}
author: ${author}
main: ${mainClass}
version: ${version}
url: ${url}
soft-depend:
  - PlaceholderAPI
depend:
  - ProtocolLib
commands:
  swiftboard:
    description: "Manage SwiftBoard"